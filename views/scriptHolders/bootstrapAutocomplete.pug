// Bootstrap Autocomplete -- musicians, bands, instruments, producers, locations
//This is the latest version that should be compatible now. Major API changes.
//It is a module and must be dynamically loaded as such
script(src="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/js/tom-select.complete.min.js")
link(href="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.bootstrap5.min.css", rel="stylesheet")
style.
  .ts-dropdown .option,
  .ts-dropdown .create,
  .ts-dropdown .no-results {
    color: var(--bs-body-color, #fff) !important;
  }
  .ts-dropdown .option.active,
  .ts-dropdown .create.active {
    background-color: var(--bs-primary) !important;
    color: var(--bs-white) !important;
  }